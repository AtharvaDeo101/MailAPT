# Read by docker compose to provision the Postgres container.
# Must match the POSTGRES_* values in backend/.env.
# Changing these after the pgdata volume exists does NOT re-provision Postgres —
# wipe the volume (docker compose down -v) or the backend will fail to connect.
POSTGRES_USER=mailapt
POSTGRES_PASSWORD=change-me
POSTGRES_DB=mailapt
